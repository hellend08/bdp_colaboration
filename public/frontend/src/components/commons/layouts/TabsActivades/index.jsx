// import Nav from 'react-bootstrap/Nav';

const TabsActividades = ({activities, activityEnabled,  activity, selectActivity}) => {
    // return (
    //     <Nav variant="pills" activeKey={activity} className="gap-3 mb-3">
    //         {
    //             activities.map( (item, index) => 
    //                 <Nav.Item key={index + 1}>
    //                     <Nav.Link eventKey={item.key} disabled={!index ? false : !(item.key === activityEnabled)}  onClick={()=> selectActivity(item.key)}
    //                     >{item.name}</Nav.Link>
    //                 </Nav.Item>
    //             )
    //         }
    //     </Nav>
    // );
}

export default TabsActividades;